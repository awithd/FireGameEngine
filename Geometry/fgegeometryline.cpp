#include "fgegeometryline.h"

/*FGE3DGeometryLine::FGEGeometryLine(glm::vec3 &pos_a, glm::vec3 &pos_b, bool vertex, bool color, bool normal)
{
    mesh.push_back(pos_a[0]);
    mesh.push_back(pos_a[1]);
    mesh.push_back(pos_a[2]);

    mesh.push_back(pos_b[0]);
    mesh.push_back(pos_b[1]);
    mesh.push_back(pos_b[2]);
}*/
